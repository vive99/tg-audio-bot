[tool.poetry]
name = "music_bot"
version = "0.1.0"
description = "Bot to send audio with images to Telegram channel"
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
aiogram = "^2.24"  # Убедись, что версия совпадает с нужной

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
